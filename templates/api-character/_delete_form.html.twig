<form method="post" action="{{ path('api_character_delete', {'identifier': character.identifier}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ character.identifier) }}">
    <button class="btn">Delete</button>
</form>
